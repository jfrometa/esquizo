name: starter_architecture_flutter_firebase
description: A new Flutter project.

publish_to: 'none'
version: 2.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  http: ^1.0.0
  async: ^2.11.0
  intl: ^0.20.2
  rxdart: ^0.28.0
  email_validator: ^2.1.17
  shared_preferences: ^2.5.2
  google_maps_flutter: ^2.10.0
  flutter_inappwebview: ^6.1.5
  #no wasm
  # speech_to_text: ^6.6.2 
  equatable: ^2.0.5
  uuid: ^4.4.2
  path: ^1.9.0
  # path_provider: ^2.1.2
  # camera: # Commented out for web/WASM compatibility
  # Note: Camera plugin can cause WASM rendering issues
  url_launcher: ^6.3.0
  localstorage: ^6.0.0
  lottie: ^3.1.3

  fl_chart: ^1.1.1
  cloud_firestore: ^6.1.2
  firebase_analytics: ^12.1.1
  firebase_auth: ^6.1.4
  firebase_auth_web: ^6.1.2
  firebase_storage: ^13.0.4
  firebase_core: ^4.4.0
  firebase_database: ^12.1.2
  firebase_ui_auth: ^3.0.1
  firebase_ui_oauth_google: ^2.0.1
  firebase_ui_oauth_apple: ^2.0.1
  firebase_ui_firestore: ^2.0.1
  firebase_messaging: ^16.1.1
  googleapis_auth: ^2.0.0
  googleapis: ^15.0.0
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  go_router: ^17.0.1

  pin_code_fields: ^8.0.1
  cupertino_icons: ^1.0.8
  # flutter_tts: ^4.0.2
  # the annotation package containing @riverpod
  google_fonts: ^8.0.0
  flutter_markdown_plus: ^1.0.4
  animated_text_kit: ^4.2.2
  flutter_dotenv: ^6.0.0
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1
  awesome_snackbar_content: ^0.1.3
  getwidget: ^7.0.0
  flutter_native_splash: ^2.4.1
  device_info_plus: ^12.3.0
  material_symbols_icons: ^4.2719.1
  geolocator: ^14.0.2
  flutter_svg: ^2.0.10+1
  google_sign_in: ^6.3.0
  # google_generative_ai: ^0.4.3
  # flutter_secure_storage: ^9.2.2

  # QR code generation and scanning
  # qr_flutter: ^4.1.0
  # mobile_scanner: ^3.5.5

  share_plus: ^12.0.1
  app_links: ^6.4.1

  animations: ^2.0.8
  flutter_animate: ^4.5.0 

  path_provider: ^2.1.2
  logger: ^2.0.2 
  qr: ^3.0.2
  firebase_app_check: ^0.4.1+4
  firebase_crashlytics: ^5.0.7
  web: ^1.1.1
  js: ^0.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  random_string: ^2.3.1
  flutter_lints: ^6.0.0
  build_web_compilers: ^4.0.11
  fake_cloud_firestore: ^4.0.1
  # a tool for running code generators
  # dart run build_runner build --delete-conflicting-outputs
  build_runner: ^2.5.4
  mockito: ^5.4.6
  # the code generator
  riverpod_generator: ^2.4.0
  # riverpod_lint makes it easier to work with Riverpod
  riverpod_lint: ^2.3.13
  # import custom_lint too as riverpod_lint depends on it
  custom_lint: ^0.7.6
  icons_launcher: ^3.0.0

flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/
    - assets/appIcon.png
    - assets/kako-logo.png
    - assets/dishes/
    - assets/catering/
    - assets/onboarding_3.svg
    - assets/onboarding_2.svg
    - assets/onboarding_1.svg
    - assets/animations/
    
    # Include all dishes images
    - assets/dishes/

  # Configure fonts to include SocialIcons from firebase_ui_auth
  fonts:
    - family: SocialIcons
      fonts:
        - asset: packages/firebase_ui_auth/fonts/SocialIcons.ttf